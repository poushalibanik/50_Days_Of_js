// In this coding challenge, you will be implementing a function called getValues(object) that 
// takes an argument: an object. The function should return an array will all of the object’s 
// values.

// Instructions:-
// If the object has no values, return an empty array.

//Solution:-

export const getValues = (object) => {
	return Object.values(object);
}
